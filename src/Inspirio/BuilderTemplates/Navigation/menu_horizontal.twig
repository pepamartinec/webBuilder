{% container Menu %}
<nav class="menu horizontal">
	<ul>
	{% for item in menuItems %}
		<li><a href="{{ BASE_HREF }}{{ item.urlName }}" title="{{ item.title }}">{{ item.title }}</a></li>
	{% endfor %}
	</ul>
</nav>